// small page for myself to try/test some things regarding tailwind etc.without breaking/bloating the main files. In prod I obviously would not commit such file but for this  application it might be usefull.
import BoxSkeleton from '../components/experiments/BoxSkeleton';

export default function test() {
	return (
		<>
			<BoxSkeleton />
		</>
	);
}
